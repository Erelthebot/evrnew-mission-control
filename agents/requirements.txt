# EVRNEW Marketing Agents — Python dependencies
# Install into the project venv: source ~/evrnew-marketing/.venv/bin/activate && pip install -r requirements.txt

crewai>=0.80.0
crewai-tools>=0.15.0
anthropic>=0.40.0
langchain-anthropic>=0.3.0
openai>=1.55.0          # xAI Grok uses OpenAI-compatible API
httpx>=0.27.0
python-dotenv>=1.0.0
sendgrid>=6.11.0
browserbase>=1.1.0
google-maps-services-python>=4.10.0
requests>=2.32.0
